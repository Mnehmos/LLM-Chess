{
  "identifier": "default",
  "description": "Default capabilities for LLM Chess",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    "shell:allow-open",
    "shell:allow-kill",
    "shell:allow-stdin-write"
  ]
}
